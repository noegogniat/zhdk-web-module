Load the provided font files and typeset the page to match the reference image.

- Use the semantic tags already in the HTML
- Headings are uppercase and the same size as the body text
- Running text has a 2ch indent, except the first paragraph
- The caption uses the mono cut

+ Hints:
- The font files are in the fonts folder, but they are not linked yet.
- Measure letter-spacing and line-height in DevTools if needed.
